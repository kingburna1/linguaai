
# requirements-dev.txt — Development only (not for production)
# Install with: pip install -r requirements-dev.txt


pytest==8.2.0
# Run your tests with: pytest

pytest-asyncio==0.23.6
# Lets pytest test async FastAPI routes properly.

httpx==0.27.0
# Used by pytest to make test HTTP requests to your API.

black==24.4.1
# Auto-formats your Python code to a consistent style.
# Run with: black app/

isort==5.13.2
# Sorts your imports alphabetically automatically.
# Run with: isort app/

flake8==7.0.0
# Checks your code for style errors and potential bugs.

pre-commit==3.7.1
# Runs black + isort + flake8 automatically before every git commit.